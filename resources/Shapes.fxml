<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.layout.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.canvas.Canvas?>

<AnchorPane xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="controller.ShapesController"
            stylesheets="@/style.css"
            styleClass="gradient-success">

    <children>
        <VBox spacing="10" AnchorPane.topAnchor="10" AnchorPane.leftAnchor="10"
              AnchorPane.rightAnchor="10" AnchorPane.bottomAnchor="10">
            <HBox spacing="10" styleClass="card shadow-md">
                <Button text="Добавить круг" onAction="#addCircle"
                        styleClass="button button-secondary shadow-sm"/>
                <Button text="Добавить прямоугольник" onAction="#addRect"
                        styleClass="button shadow-sm"/>
                <Button text="Очистить холст" onAction="#clearCanvas"
                        styleClass="button button-danger shadow-sm"/>
                <Button text="Назад" onAction="#goBack"
                        styleClass="button button-ghost shadow-sm"/>
            </HBox>
            <Canvas fx:id="canvas" width="600" height="400"
                    styleClass="canvas shadow-lg"/>
        </VBox>
    </children>
</AnchorPane>